/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      'localhost',
      'wolfgeekmarkt-production.up.railway.app'
    ],
  },
};

module.exports = nextConfig;
